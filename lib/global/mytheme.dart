import 'package:flutter/material.dart';
import 'dart:ui';

class MyTheme {
  //按屏幕尺寸计算大小
  static double sz( double size) {
    return size;
  }

  static const Color colorDark = Colors.black54;
  static const Color color = Colors.pink;
  static const Color accentColor = Colors.pinkAccent;
  static const Color bgColor = Colors.white;
  static const Color fontColor = Colors.black54;
  static const Color fontDeepColor = Colors.black87;
}
